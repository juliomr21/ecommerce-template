import { DinamicFormatPipe } from './dinamic-format.pipe';

describe('DinamicFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DinamicFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
